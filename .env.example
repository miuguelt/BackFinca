## Entorno de ejemplo (.env)
# Copia este archivo como `.env` y ajusta valores.

# -----------------------
# Selección de entorno
# -----------------------
# development | production | testing
FLASK_ENV=development

# -----------------------
# Base de Datos
# -----------------------
DB_HOST=isladigital.xyz
DB_PORT=3311
DB_NAME=finca
DB_USER=fincau
DB_PASSWORD=fincac
# (Opcional) Usar URI completa en lugar de variables separadas
# SQLALCHEMY_DATABASE_URI=mysql+pymysql://root:changeme@localhost:3306/finca

# -----------------------
# Redis (configuración unificada)
# -----------------------
# Formato: redis://[:password]@host:port/db
# Dev/Prod: usar DB 0 por defecto
REDIS_URL=redis://localhost:6379/0

# Overrides específicos (opcionales)
# Si no los defines, se usan los valores de REDIS_URL
CACHE_REDIS_URL=
RATE_LIMIT_STORAGE_URI=

# Testing: por defecto usa DB 2 (para aislar datos de dev)
TEST_REDIS_URL=redis://localhost:6379/2
TEST_CACHE_REDIS_URL=
TEST_RATE_LIMIT_STORAGE_URI=

# -----------------------
# JWT y cookies
# -----------------------
# En producción DEBE estar definido y ser seguro (64 chars hex recomendados)
JWT_SECRET_KEY=replace_with_secure_hex_64
# Dominio de cookies en producción (ej: .isladigital.xyz)
JWT_COOKIE_DOMAIN=

# -----------------------
# CORS
# -----------------------
# CSV o JSON Array
# CORS_ORIGINS=["https://finca.isladigital.xyz","http://localhost:5173"]
CORS_ORIGINS=https://localhost:5173,https://localhost:5180, https://localhost:5174, https://localhost:5175, https://localhost:5176, https://localhost:5179

# -----------------------
# URLs (opcionales)
# -----------------------
API_BASE_URL=/api/v1
FRONTEND_URL=https://localhost:5180

# -----------------------
# Otros
# -----------------------

BACKEND_URL=https://localhost:8081/api/v1