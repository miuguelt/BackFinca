{
  "info": {
    "name": "FincaBack - Endpoints (cookies y bearer)",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Auth Login",
      "request": {
        "method": "POST",
        "header": [
          { "key": "Content-Type", "value": "application/json" }
        ],
        "body": {
          "mode": "raw",
          "raw": "{\n  \"identifier\": \"99999999\",\n  \"password\": \"password123\"\n}"
        },
        "url": { "raw": "{{baseUrl}}/api/v1/auth/login" },
        "description": "Autenticación para obtener access_token y cookies. Guarda access_token en variables de colección."
      },
      "event": [
        {
          "listen": "test",
          "script": {
            "type": "text/javascript",
            "exec": [
              "let json = null;",
              "try { json = pm.response.json(); } catch (e) {}",
              "if (json && json.data && json.data.access_token) { pm.collectionVariables.set('access_token', json.data.access_token); }",
              "pm.test('status 200', function () { pm.response.to.have.status(200); });",
              "pm.test('access_token guardado', function () { pm.expect(pm.collectionVariables.get('access_token')).to.be.a('string'); });"
            ]
          }
        }
      ]
    },
    {
      "name": "Treatments (cookies)",
      "request": {
        "method": "GET",
        "header": [],
        "url": { "raw": "{{baseUrl}}/api/v1/treatments" },
        "description": "Requiere haber iniciado sesión antes (POST {{baseUrl}}/api/v1/auth/login) para que Postman envíe las cookies automáticamente."
      }
    },
    {
      "name": "Treatments (Bearer {{access_token}})",
      "request": {
        "auth": {
          "type": "bearer",
          "bearer": [ { "key": "token", "value": "{{access_token}}", "type": "string" } ]
        },
        "method": "GET",
        "header": [],
        "url": { "raw": "{{baseUrl}}/api/v1/treatments" },
        "description": "Usa el token guardado en la variable de colección {{access_token}}. Ejecuta primero 'Auth Login'."
      }
    },
    {
      "name": "Vaccines",
      "item": [
        {
          "name": "List (minimal fields)",
          "request": {
            "auth": {
              "type": "bearer",
              "bearer": [ { "key": "token", "value": "{{access_token}}", "type": "string" } ]
            },
            "method": "GET",
            "header": [],
            "url": { "raw": "{{baseUrl}}/api/v1/vaccines?fields=id,name&limit=50&page=1" },
            "description": "Lista de vacunas con proyección de columnas (id,name) para máxima eficiencia."
          }
        },
        {
          "name": "By Route (minimal fields)",
          "request": {
            "auth": {
              "type": "bearer",
              "bearer": [ { "key": "token", "value": "{{access_token}}", "type": "string" } ]
            },
            "method": "GET",
            "header": [],
            "url": { "raw": "{{baseUrl}}/api/v1/vaccines/by-route/{{route_id}}?fields=id,name,route_administration_id&limit=50&page=1" },
            "description": "Filtra por vía de administración. Define la variable de entorno de colección route_id antes de ejecutar."
          }
        },
        {
          "name": "With Route Administration (minimal)",
          "request": {
            "auth": {
              "type": "bearer",
              "bearer": [ { "key": "token", "value": "{{access_token}}", "type": "string" } ]
            },
            "method": "GET",
            "header": [],
            "url": { "raw": "{{baseUrl}}/api/v1/vaccines/with-route-administration?fields=id,name,route_administration_id&limit=25&page=1" },
            "description": "Incluye relación route_administration precargada con selectinload."
          }
        }
      ]
    }
  ],
  "variable": [
    { "key": "baseUrl", "value": "https://127.0.0.1:8081" },
    { "key": "route_id", "value": "1" },
    { "key": "access_token", "value": "" }
  ]
}