# Core Flask dependencies
blinker==1.9.0
click==8.2.1
colorama==0.4.6
Flask==3.1.2
Flask-Cors==6.0.1
Flask-JWT-Extended==4.7.1
Flask-SQLAlchemy==3.1.1
flask-restx==1.3.0
Flask-Limiter==3.12.0
Flask-Migrate==4.1.0
Werkzeug==3.1.3
alembic==1.13.2

# Database dependencies
SQLAlchemy==2.0.43
PyMySQL==1.1.2
# mysqlclient==2.2.4  # Opcional (requiere Build Tools y no hay wheel para Python 3.13 aún)
# mysql-connector-python==9.0.0  # Alternativa pura Python (driver: mysql+mysqlconnector)
# greenlet==3.2.4  # Opcional (puede fallar compilación en Python 3.13 en Windows)

# Security dependencies
cryptography==45.0.7
PyJWT==2.10.1
bcrypt==4.2.0

# Utility dependencies
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.2
packaging==25.0
typing_extensions==4.15.0
python-dotenv==1.1.1

# Production server
gunicorn==23.0.0
gevent==23.9.1

# Performance monitoring dependencies
psutil==7.0.0

# Development and testing dependencies (optional)
# pytest==8.2.2
# pytest-flask==1.3.0
# pytest-cov==6.3.0
# black==24.4.2
# flake8==7.1.0
pytest-mock==3.14.0
freezegun==1.5.1

# Redis for advanced caching (enabled for all environments)
redis==5.0.7
Flask-Caching==2.3.0

# Optional: Celery for background tasks (uncomment if needed)
# celery==5.4.0

# Optional: Elasticsearch for advanced search (uncomment if needed)
# elasticsearch==8.14.0


